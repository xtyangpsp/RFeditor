#ifndef _GLOBAL_H
#define _GLOBAL_H
//set global variables.
const string evidkey("eventid");   // not evid to avoid collision
const string traceidkey("traceid");   // unique id for each trace
const bool MYDEBUGMODE(true); // when true, the program will output some values for checking purpose.

#endif